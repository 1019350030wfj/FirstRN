'use strict';

import React, {
    Component,
    StyleSheet,
    Text,
    View,
} from 'react-native';

export default class RouteHome extends Component {
    render() {
        return(
            <View style = {{flex:1}}>
                <Text>我的</Text>
            </View>
        );
    }
}