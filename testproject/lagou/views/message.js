'use strict';

import React, {
    Component,
    StyleSheet,
    Text,
    View,
} from 'react-native';

export default class MessagePage extends Component {
    render() {
        return(
            <View style = {{flex:1}}>
                <Text>消息</Text>
            </View>
        );
    }
}