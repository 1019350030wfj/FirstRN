'use strict';

import React,{
    StyleSheet,
} from 'react-native';

const styles = StyleSheet.create({
    container: {
        flex:1,
    },
    headerWrapper: {
        flex:4,
    },
    editorWrapper: {
        position:'absolute',
        bottom:'0',
        left:'0',
        right:''
    }
});

export default styles;